server_configuration:
  num_server_rounds: 100
  local_epochs: 10
  num_supernodes: 800
  partition: "dirichlet"
  fraction: 8

  #  flower-simulation --app . --num-supernodes 800 --run-config 'num-server-rounds=100 strat-mode="fed_avg" local-epochs=10 partition="dirichlet"'

