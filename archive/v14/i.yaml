
server_configuration:
  num_server_rounds: 100
  local_epochs: 10
  num_supernodes: 100
  partition: "iid"
  fraction: 16

# # flower-simulation --app . --num-supernodes 100 --run-config 'num-server-rounds=100 local-epochs=10 partition="iid" fraction=16 strat-mode="fed_avg"'
