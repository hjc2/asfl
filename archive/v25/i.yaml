server_configuration:
  num_server_rounds: 200
  local_epochs: 5
  num_supernodes: 200
  partition: "iid"
  fraction: 2

# flower-simulation --app . --num-supernodes 200 --run-config 'num-server-rounds=200 local-epochs=5 partition="dirichlet" fraction=2 strat-mode="fed_avg"'